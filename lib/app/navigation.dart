import 'package:flutter/widgets.dart';

/// Root navigator key dùng cho GoRouter + điều hướng global (reset password, v.v.)
final GlobalKey<NavigatorState> rootNavigatorKey = GlobalKey<NavigatorState>();
